// Output: 3
// hello world
Var Add(one, two) {
    RETURN one + two;
};
Void MyPrint(message) {
    PRINT(message);
    RETURN;
};
Var one = 1;
MyPrint(Add(one, 2));
MyPrint(Add("hello", " world"));
